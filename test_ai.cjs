const http = require("http"); const req = http.request({hostname: "localhost", port: 5000, path: "/api/problems/ai-analyze", method: "POST", headers: {"Content-Type": "application/json"}}, (res) => { let data = ""; res.on("data", d => data += d); res.on("end", () => console.log("Response:", data)); }); req.write(JSON.stringify({text: "Main water pipeline broken", latitude: 23.3, longitude: 85.3})); req.end();
